apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "akkoma.fullname" . }}-test-connection"
  labels:
    {{- include "akkoma.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "akkoma.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
