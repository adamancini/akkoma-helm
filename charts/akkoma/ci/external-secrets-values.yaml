# CI test: External secrets mode
akkoma:
  domain: test.example.com
  adminEmail: admin@test.example.com

externalSecret:
  enabled: true
  name: my-akkoma-secrets
  keys:
    secretKeyBase: secret-key-base
    signingSalt: signing-salt
    releaseCookie: release-cookie
